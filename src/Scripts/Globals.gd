extends Object
class_name Globals

enum Stages {release, testing, dev}
enum ItemType {item, map_object, npc}

static var data_compatibility: String = "0.1.0"

static var current_stage: Stages = Stages.dev
static var full_version_name: String = "TGPY-indev"
